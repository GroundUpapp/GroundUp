{
  "name": "ground-up",
  "version": "0.1.0",
  "private": true,
  "description": "Ground Up — financial dashboard for contractors (monorepo root).",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client"
  }
}
